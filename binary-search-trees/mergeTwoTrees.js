function convertToDLL(root, head) {
  if (!root) {
    return null;
  }
  ri;
}
function mergeTwoTrees(root1, root2) {
  //1. Convert a BST to LL
  //2. Merge two Sorted LL
  //3. Convert LL to BST
}
